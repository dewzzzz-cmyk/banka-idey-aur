# PostgreSQL
POSTGRES_USER=ideaportal
POSTGRES_DB=ideaportal
POSTGRES_PASSWORD=changeme123
DATABASE_URL=postgresql://ideaportal:changeme123@postgres:5432/ideaportal

# API
NODE_ENV=development
API_PORT=3000
SESSION_SECRET=changeme_min32chars_replace_in_prod_x7k2m9

# ─── AI Provider (choose ONE) ──────────────────────────────────────
# Option A: DeepSeek API (cloud, no GPU needed, ~$0.001/1K tokens)
DEEPSEEK_API_KEY=           # get at platform.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# Option B: Ollama (local, needs GPU or slow CPU, free)
# Leave DEEPSEEK_API_KEY empty to use Ollama
# Start with: docker compose --profile ollama up
OLLAMA_MODEL=qwen2.5:7b-instruct-q5_K_M
OLLAMA_EMBED_MODEL=nomic-embed-text
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_NUM_PARALLEL=1
OLLAMA_MAX_LOADED_MODELS=1

# Optional
LDAP_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
TELEGRAM_BOT_TOKEN=
